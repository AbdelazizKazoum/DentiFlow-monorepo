/**
 * Input data for updating an existing insurance template.
 * All fields are optional to allow partial updates.
 */
export interface UpdateInsuranceTemplateInput {
  name?: string;
  fileUrl?: string;
}
