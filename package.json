{
  "name": "dentiflow-monorepo",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  },
  "dependencies": {
    "next-intl": "^4.9.0",
    "@mui/icons-material": "^9.0.0"
  }
}
